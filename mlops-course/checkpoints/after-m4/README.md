# checkpoints/after-m4

workspace 在 **M4 結束**時的已知良好狀態：+ BentoML 服務 / PyTorch 模型（階 5–8）

重置指令：
```bash
cp -r checkpoints/after-m4/. workspace/
```

> 占位骨架。建置教材時用 `make checkpoint-m4` 從通過的 workspace 產生實際內容。
