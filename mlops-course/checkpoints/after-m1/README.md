# checkpoints/after-m1

workspace 在 **M1 結束**時的已知良好狀態：純 sklearn baseline + Git（階 0）

重置指令：
```bash
cp -r checkpoints/after-m1/. workspace/
```

> 占位骨架。建置教材時用 `make checkpoint-m1` 從通過的 workspace 產生實際內容。
