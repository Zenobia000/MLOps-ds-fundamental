# checkpoints/after-m2

workspace 在 **M2 結束**時的已知良好狀態：+ MLflow 追蹤 + Optuna 調參 + DVC（階 1–3）

重置指令：
```bash
cp -r checkpoints/after-m2/. workspace/
```

> 占位骨架。建置教材時用 `make checkpoint-m2` 從通過的 workspace 產生實際內容。
