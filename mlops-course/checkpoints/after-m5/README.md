# checkpoints/after-m5

workspace 在 **M5 結束**時的已知良好狀態：+ Prefect 編排 + GitHub Actions CI（階 9–10）

重置指令：
```bash
cp -r checkpoints/after-m5/. workspace/
```

> 占位骨架。建置教材時用 `make checkpoint-m5` 從通過的 workspace 產生實際內容。
