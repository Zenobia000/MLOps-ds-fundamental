# 訓練映像依賴 — 版本由 uv.lock 產生,請勿手改。
#
# 誰在讀它:docker/Dockerfile.train 會 COPY 這個檔再 pip install。
# 為什麼精確釘死:映像要「每次 build 都一樣」,而且要跟開發環境同一組版本——
#   否則會出現「本機跑得動、容器裡爆掉」。這裡的版本全部取自本專案的 uv.lock。
#
# 怎麼更新:改 pyproject.toml → uv lock → 重新產生本檔（見 README 環境章節）。

numpy==1.26.4
pandas==2.3.3
scikit-learn==1.9.0
xgboost==3.2.0
optuna==4.9.0
mlflow==3.15.0
pyyaml==6.0.3
pydantic==2.13.4
torch==2.13.0
torchvision==0.28.0
onnx==1.22.0
onnxscript==0.7.1
onnxruntime==1.28.0
pillow==12.3.0
