# ⚠️ 這份是「Bento 映像」的依賴清單,不是給你的課程環境用的。
#
# 誰在讀它:bentofile.yaml 的 `requirements_txt:` 指向這裡,
#   bentoml build 時會把這些版本烤進 bento。
# 為什麼釘死版本:交付物要每次 build 都一致,所以用 == 精確鎖定。
#
# ★ 不要在你的課程 venv 裡跑 pip install -r 這個檔 ★
#   課程環境(mlops-course/uv.lock)已經有 bentoml/sklearn,重裝只是多此一舉。
#
# ★ 這裡的版本必須跟課程環境一致 ★
#   Model Store 裡的 sklearn 模型是 pickle。你在本機用 A 版訓練存檔,
#   bento 裡卻裝 B 版載入,輕則 InconsistentVersionWarning,重則反序列化失敗——
#   「本機好好的,進 image 就掛」的典型死法。
#   升級課程環境時記得同步這裡,這正是 m2 在教的「鎖版本才可重現」。
#
# 03_bentoml 沙盒依賴(對齊 uv.lock,2026-08-02 校對)
bentoml==1.4.39
scikit-learn==1.9.0
numpy==1.26.4
