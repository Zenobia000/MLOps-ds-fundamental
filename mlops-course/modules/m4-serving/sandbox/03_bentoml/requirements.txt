# ⚠️ 這份是「Bento 映像」的依賴清單,不是給你的課程環境用的。
#
# 誰在讀它:bentofile.yaml 的 `requirements_txt:` 指向這裡,
#   bentoml build 時會把這些版本烤進 bento。
# 為什麼釘死版本:交付物要每次 build 都一致,所以用 == 精確鎖定。
#
# ★ 不要在你的課程 venv 裡跑 pip install -r 這個檔 ★
#   版本與 mlops-course/uv.lock 不同,裝下去會把課程環境降級。
#   要在本機跑 service.py,課程環境已經有 bentoml/sklearn。
#
# 03_bentoml 沙盒依賴
bentoml==1.3.4
scikit-learn==1.5.2
numpy==2.1.1
